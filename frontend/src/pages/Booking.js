import React from 'react';
import { Container, Typography } from '@mui/material';

function Booking() {
  return (
    <Container sx={{ mt: 4 }}>
      <Typography variant="h5">Booking Page</Typography>
      <Typography>Booking functionality coming soon.</Typography>
    </Container>
  );
}

export default Booking;